var assert = require('assert');
function evaluateClassScope(stack, properties, layers, classes) {
    assert(layers.length == 0);
    assert(classes.length == 0);
    return properties;
}
module.exports = evaluateClassScope;
//# sourceMappingURL=class.js.map