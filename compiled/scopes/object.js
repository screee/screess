var assert = require('assert');
function evaluateObjectScope(stack, properties, layers, classes) {
    assert(layers.length == 0);
    assert(classes.length == 0);
    return properties;
}
module.exports = evaluateObjectScope;
//# sourceMappingURL=object.js.map